{
  "npmClient": "yarn",
  "version": "independent",
  "packages": ["packages/*"],
  "useWorkspaces": true,
  "command": {
    "version": {
      "exact": true,
      "npmClient": "yarn"
    },
    "publish": {
      "npmClient": "yarn",
      "allowBranch": [
        "canary"
      ],
      "registry": "https://registry.npmjs.org/"
    }
  }
}
